extends Node

enum TYPE { 
	SCP_001, 
	SCP_002
}

enum RESEARCH_LVL { 
	INITIAL, ONE, TWO, THREE
}

enum EVENT_TYPE {
	AFTER_CONTAINMENT,
	DURING_TRANSFER,
	AFTER_TRANSFER,
	RANDOM_EVENTS,
	BEFORE_DESTROY,
	AFTER_DESTROY,
	# -----------
	START_TESTING,
	DURING_TESTING
}
